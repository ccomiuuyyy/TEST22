# LLM에게 바이트 단위 지시는 통하지 않는다

## 자르는 쪽은 알고, 읽는 쪽은 모른다

| | 바이트를 세는가 |
|---|---|
| 수집·전처리 코드 (PHP, Python 등) | **센다.** `substr($text, 0, 500)` 로 정확히 자름 |
| LLM (Gemini, GPT, Claude) | **못 센다.** 자가 없음 |

LLM은 글자를 토큰 덩어리로 처리한다. "뒤에서 500바이트"라고 지시해도
어디가 그 경계인지 계산할 수 없고, 세는 척하며 대충 짐작한다.

## 그래서 프롬프트의 바이트 지시는 두 종류로 갈린다

**이미 잘린 데이터를 받는 경우 — 무해하지만 불필요**

```
근거문맥 : 2순위 Context Stitching (키워드 앞뒤 결합, 최대 2000byte)
```

전처리 단계에서 이미 잘라 붙였고 모델은 결과물만 받는다.
이 문장은 지시가 아니라 벌어진 일의 설명이다. 없어도 동작이 같다.

**전문을 주고 범위를 지정하는 경우 — 실행 불가**

```
본문전체 : 2순위 기사 후방 500byte, 3순위 기사 전방 100byte
```

모델은 그 경계를 찾지 못한다. "뒷부분을 좀 더 보라는 거구나" 정도로
흐릿하게 받아들일 뿐이다.

## 모델이 할 수 있는 것 / 없는 것

| 지시 | 되나 |
|---|---|
| 특정 단어의 앞 / 뒤 구분 | 됨 |
| 그 단어가 든 문장 | 됨 |
| 그 다음 한두 문장 | 됨 |
| 같은 문단 안 | 됨 |
| 기사 후반부 / 첫 문장 | 됨 |
| 500바이트 | 안 됨 |
| 대략 5줄쯤 | 흐릿함 |

**문장·문단 단위면 정확하고, 숫자로 재는 것은 못 한다.**
사람도 "이 단어 다음 두 문장"은 세지만 "다음 500바이트"는 못 세는 것과 같다.

## 해결 — 숫자 대신 앵커를 준다

바이트 숫자는 수단이었고, 진짜 목적은 "어디를 중점적으로 봐라"였다.
그 목적은 **입력 데이터에 기준점을 넣어주면** 더 잘 달성된다.

```json
{
  "article_id": "10084449",
  "target_mode": "근거문맥",
  "matched_keyword": "노바티스",
  "title": "...",
  "context": "..."
}
```

두 항목이 각각 다른 문제를 푼다.

**`target_mode`** — 프롬프트에 "근거문맥일 때는 A, 본문전체일 때는 B" 식
분기가 있어도, 데이터에 지금 어느 모드인지 표시가 없으면 모델은 어느 쪽을
따를지 모른다. 모드를 알려주면 분기가 실행된다.

**`matched_keyword`** — "키워드 직후의 내용에 집중하라" 같은 지시는
어느 단어가 키워드인지 알아야 실행된다. 없으면 모델이 짐작한다.

## 왜 바이트보다 나은가

키워드 앞뒤를 이어붙인 데이터라면, **이어붙인 자리가 키워드 근처**다.

```
...앞부분 조각... [키워드] ...뒷부분 조각...
                     ↑
                  접합부
```

"뒤에서 500바이트"로는 못 찾지만, 키워드를 알려주면 접합부를 바로 찾는다.
문맥이 급격히 전환되는 지점이 어디인지도 함께 파악된다.

## 정리

- 프롬프트에 바이트·글자수를 쓰지 말고 **문장·문단·상대 위치**로 쓴다
- 범위 지정이 필요하면 숫자 대신 **앵커(키워드, 모드)를 데이터에 넣는다**
- 프롬프트를 고치는 것보다 **입력 데이터를 채우는 쪽이 위험이 낮다**
