# 의견 요청 사항

> 아래 내용에 대한 의견을 구합니다.

## 1. ROLE 관련 현황

| 구분 | 내용 |
|---|---|
| 현황 | 현재 시스템 프롬프트에 ROLE이 2개 입력됨 (분석가, 애널리스트) |
| 근거 | 새로 정의 시 "이전 무시" 명령어가 있어도, 이미 입력된 내용이 있으면 영향을 받음 |
| 제안 | 시스템 프롬프트에서 ROLE 미사용 |

### 문의 사항
- 예전 시스템 프롬프트에서 ROLE 미사용을 권장하지 않는 개발팀 의견이 있었는데, 유저 프롬프트 사용 시에도 동일한지 문의
- 위 2개 ROLE + 유저 프롬프트 ROLE 사용 시 총 3개가 되는데, 분석에 영향을 끼치는지 문의

### 참고
- 최근 분석 시 Business Data Analyst가 적용되는 것으로 추측됨
- **reason** : "비즈니스 활동으로 보기 어렵습니다", "비즈니스 액션과는 거리가 있음" 등의 답변이 노출됨(이것이 영향받는다는 의미가 아닐지)


### 타팀의 의견이 필요할 시
- 각 팀과 각 고객사별 ROLE 사용을 추구하는 경향으로 알고 있는데 시스템 프롬프트내 ROLE 사용을 원하는지 필요하시면 투표를 하겠습니다.








# AI 답변 근거 LLM 프롬프트에서 ROLE이 여러 개일 때

## 자리와 내용은 다른 축이다

LLM API 요청에는 텍스트를 넣는 자리가 두 개 있다.

| | 뜻 |
|---|---|
| **시스템 지시** | 어디에 쓰느냐 — 자리 |
| **ROLE** | 무엇을 쓰느냐 — 내용 |

ROLE("너는 ~하는 사람이다")은 시스템 자리에도 유저 자리에도 쓸 수 있다.
같은 자리에 출력 형식이나 판단 기준 같은 다른 내용도 함께 들어간다.

## ROLE이 세 군데에 흩어질 수 있다

실제 구성에서 ROLE 성격의 문장이 이렇게 나뉘어 있었다.(양희철기준)

| 위치 | 성격 |
|---|---|
| `system_instruction` | 시스템 자리, 추상적 |
| 시스템 프롬프트 변수 | 시스템 자리, 추상적 |
| 공통 프롬프트 | 유저 자리, 구체적 |

세 문장이 서로 다른 역할을 지정해도 **어느 하나가 나머지를 무시하지 않는다.**
변수 할당처럼 덮어쓰는 메커니즘이 없어서, 모델은 셋을 모두 읽고 섞어서 행동한다. (시스템프롬프트파일만 봤을때) ai_schema.inc

## 무엇이 이기는가

두 가지 축이 함께 작용한다.

| 축 | 강한 쪽 |
|---|---|
| 자리 | 시스템 > 유저 |
| 구체성 | 구체적 > 추상적 |

둘이 부딪히면 **구체적인 쪽이 이길 가능성이 높다.** 보장은 아니다.

시스템 자리에 "비즈니스 분석가로 행동하라" 같은 추상적 규정이 있고,
유저 자리에 "제목에 A, B, C 중 하나가 없으면 1점" 같은 구체적 규칙이 있으면
실제 판정은 유저 쪽 규칙이 지배한다.

## 실무적 의미

시스템 자리를 고칠 수 없는 환경이어도 큰 제약은 아니다.
거기 있는 것이 대체로 추상적인 역할 규정이라면,
실제 결과를 좌우하는 것은 구체적으로 쓴 유저 프롬프트다.

다만 ROLE이 여러 개인 상태가 바람직하지는 않다.
고칠 수 있는 쪽에서 중복을 줄이는 편이 낫다.

## 예외 — 구조로 강제한 것

프롬프트가 아니라 **스키마로 박아둔 것**은 자리·구체성과 무관하게 이긴다.

```
responseSchema 로 "JSON 배열로만 답하라"를 지정하면
유저 프롬프트에서 "표로 정리해줘"라고 해도 JSON이 나온다.
```

지시 준수 문제를 프롬프트 설득이 아니라 출력 형식 자체로 막는 방식이라
같은 목적이면 이쪽이 훨씬 확실하다.
